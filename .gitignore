# Python caches / build artifacts (reproducible from sources, not committed).
# Spike-specific ignores live in spike/.gitignore.
__pycache__/
*.pyc
.venv/
freerouting.jar          # CI downloads this into the route job; never commit it
