# Generated build artifacts & environments — reproducible from sources, not committed
.venv/
__pycache__/
*.pyc
build/
**/elec/layout/
kicad-symbols/
lib_sym_cache*
*_sklib.py
*.erc
*.log
skidl_REPL.*
*.net
# NOTE: per-part 3D models under elec/src/parts/**/*.step are COMMITTED (real part
# data, for reproducible ato build in CI). Build-output .step lives under build/ and
# **/elec/layout/ which are already ignored above, so we don't blanket-ignore *.step.
generated.ato
gen_*.ato
